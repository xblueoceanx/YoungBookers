<% if @review.errors.any? %>
  <div class="error_explanation">
  <h2><%= @review.errors.count %> errors prohibited this obj from being saved:</h2>
    <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div class="box-myr col-md-6 col-md-offset-3">
  <h2 class="t-myr"><strong>マイレビュー編集</strong></h2>
    <%= form_for(@review) do |f| %>
      <div>
        <label class="m-b-tr">本のタイトル</label>
          <div>
            <%= f.text_field :title, autofocus: true, class: "col-xs-12" %>
          </div>
      </div>
      <div>
        <label class="m-b-tr">本のレビュー</label>
          <div>
            <%= f.text_area :book_review, autofocus: true, class: "col-xs-12" %>
          </div>
      </div>
      <!-- ボタン群 -->
      <p class="up-btn">
        <%= f.submit "更新する",  class: "box-up botn btn btn-primary btn-block" %>
      </p>
    <% end %>
</div>